import 'package:flutter/material.dart';

/// The root navigator, for the few things that have to move the app from
/// outside a widget — a session ending, say.
final appNavigatorKey = GlobalKey<NavigatorState>();
